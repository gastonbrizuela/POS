import React from "react";

/* Custom Components */
import PayForms from "./PayForms";

const PayFormsContainer = ({ payForms }) => {
  return <PayForms payForms={payForms} />;
};

export default PayFormsContainer;
